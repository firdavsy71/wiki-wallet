package com.wiki.wallet.core.designsystem.theme

import androidx.compose.ui.graphics.Color

object WalletColors {
    val Coral         = Color(0xFFF4737B)  // primary accent, gain/loss chips, receive card
    val CoralDeep     = Color(0xFFE85F68)  // pressed state
    val CoralSoft     = Color(0xFFFAA9AE)  // gradient tail on slider
    val Ink           = Color(0xFF1C1C1E)  // dark card / dark screen bg
    val InkElevated   = Color(0xFF2A2A2C)  // nested card on dark
    val Paper         = Color(0xFFF2F1EF)  // light card bg
    val PaperPure     = Color(0xFFFAFAF8)  // light screen bg
    val Canvas        = Color(0xFFD9D9D9)  // outer app frame / mockup bg
    val MintChip      = Color(0xFF34D399)  // positive delta dot
    val TextPrimary   = Color(0xFF141416)
    val TextOnDark    = Color(0xFFF5F5F5)
    val TextMuted     = Color(0xFF8E8E93)
    val TextMutedDark = Color(0xFF6E6E73)
    val HatchStroke   = Color(0x1A000000)  // diagonal hatch on inactive bars
    val CardBorder    = Color(0x0D000000)  // subtle 1dp border for light cards
}
